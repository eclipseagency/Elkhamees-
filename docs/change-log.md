# سجل التغييرات

## 2026-07-12 — واجهة المتجر العربية

- **تم:** تحويل الصفحة التعريفية إلى واجهة متجر RTL تركز على المنتجات.
- **تم:** إضافة التصنيفات، فلاتر المنتجات، المفضلة، السلة، والطلب عبر واتساب.
- **تم:** إضافة تجربة مناسبات، عناصر ثقة، شحن، تغليف، وفوتر تجاري كامل.
- **تم:** التحقق على سطح المكتب والجوال دون أخطاء أو تمرير أفقي.
- **مؤقت:** صور وأسعار المنتجات الحالية للعرض فقط؛ تستبدل ببيانات الكتالوج المعتمدة قبل الإطلاق.
- **مراجعة مطلوبة:** تأكيد رقم واتساب، عنوان الفرع، ساعات العمل، وسياسات الشحن والاستبدال.

## 2026-07-13 — خيارات مرجعية للصفحة الرئيسية

- **تم:** إنشاء خيارين خفيفين ومستقلين للصفحة الرئيسية: اتجاه تحريري فاتح، واتجاه فاخر داكن.
- **تم:** التحقق بصريًا من الخيارين على سطح المكتب والجوال بعرض 390px دون تمرير أفقي.
- **بدون تغيير:** الصفحة الرئيسية الحالية بقيت كما هي، ولم يتم نشر أي من الخيارين.
- **نُشر لاحقًا:** أُضيف الخياران كمعاينات عامة تحت `/concepts/editorial/` و`/concepts/noir/` على مشروع Vercel نفسه، مع بقاء الصفحة الرئيسية الحالية دون تغيير بصري.
- **تحسين:** أُعيد بناء الخيارين كتجربتي متجر متكاملتين مستلهمتين وظيفيًا من متاجر المجوهرات الكبرى، مع صور حملة ومنتجات أصلية مولّدة للمشروع، وأقسام فئات ومنتجات وثقة ومناسبات.
- **تحقق:** اختُبرت النسختان على سطح المكتب والجوال، ونُشرت الصور محليًا داخل مشروع Vercel بدل الاعتماد على مصادر صور خارجية.
- **تحسين الكتالوج:** استُبدلت أسماء المنتجات الشعرية بأسماء وصفية تتبع نمط المتاجر الاحترافية (نوع القطعة، التصميم أو الحجر، وعيار الذهب)، وأضيفت صور ومنتجات للسوار والطقم والقلادة الدائرية.

## 2026-07-15 — اعتماد اتجاه Noir

- **اعتماد العميل:** اختيرت نسخة Noir المنشورة على `/concepts/noir/` كأساس للموقع.
- **ترقية آمنة:** أصبح المسار الرئيسي `/` يحوّل إلى نسخة Noir عبر Vercel، مع إبقاء `index.html` السابق محفوظًا للرجوع دون حذف.
- **مرجع بصري:** المرجع الصحيح هو الصفحة المنشورة الحالية ذات الخلفية السوداء وصورة الحملة باللون العنابي؛ صور المعاينة القديمة داخل `concepts/previews/` لم تعد مرجعًا نهائيًا.
- **قبل الإطلاق التجاري:** يلزم اعتماد الكتالوج والأسعار ورقم التواصل وعنوان المعرض وسياسات الشحن والاستبدال.
- **تحسين إنتاجي:** أزيلت جميع الروابط الوهمية، وربطت عناصر التنقل بالأقسام الفعلية، وأضيفت بيانات الوصف والمشاركة الاجتماعية والرابط الأساسي وتحسينات التركيز بلوحة المفاتيح.
- **تصحيح معلومات:** أزيل افتراض أن المعرض في الرياض إلى أن يعتمد العميل العنوان والمواعيد رسميًا.
- **تحقق:** اجتاز المسار الرئيسي فحص Playwright بعرضي 390px و1440px دون تمرير أفقي أو أخطاء متصفح أو طلبات أصول فاشلة.
- **تجربة الجوال:** أضيف شريط تنقل واضح للجوال بدل إخفاء روابط الأقسام بالكامل.
- **رحلة المنتج:** أضيف رابط طلب تفاصيل لكل قطعة مع تحميل كسول لصور المنتجات، دون اختلاق سعر أو قناة تواصل غير معتمدة.

## 2026-07-20 — مستند خريطة الموقع والنطاق

- **جديد:** أُعد مستند "خريطة الموقع والغرض من كل قسم" بناءً على طلب العميل توضيح وظيفة كل قسم قبل بداية التنفيذ، لتقليل التعديلات لاحقًا.
- **الملفات:** `docs/sitemap.md` (النص المصدر)، `docs/sitemap-deck.html` (التصميم)، `docs/AlKhamees-Sitemap.pdf` (خمس صفحات A4 بهوية Noir، خط Amiri للعناوين وTajawal للنص).
- **قرار مقترح للاعتماد:** الموقع واجهة عرض تنتهي بواتساب، بدون عربة شراء أو دفع إلكتروني في المرحلة الأولى.
- **بيانات الفرع:** مأخوذة من قوقل مابس بتاريخ 2026-07-20 (العليا، الرياض · السبت إلى الخميس ٣ عصرًا حتى ١١ مساءً · الجمعة إجازة)، **ولم يعتمدها العميل بعد**. يبقى تحفظ 2026-07-15 قائمًا حتى التأكيد الرسمي.
- **تعارض قائم:** رقم قوقل مابس ٩٦٦٥٥٢١٨٨٨٢٢ يخالف رقم الواتساب في الموقع ٩٦٦١٣٨٣٢٠٨١٤. يلزم تحديد الرقم المعتمد.

## 2026-07-20 — تنبيه: مشروعان على Vercel لنفس المستودع

- **الوضع:** كان هذا المستودع مربوطًا بمشروعَي Vercel في نفس الوقت: `alkhamees-jewellery` (يعمل) و`elkhamees` (يفشل دائمًا).
- **سبب الفشل:** إعداد `elkhamees` كان Next.js بينما المشروع موقع ثابت بلا `package.json`، فكان كل دفع ينتج خطأ `No Next.js version detected` ورسالة فشل بالبريد.
- **الإجراء:** فُصل ربط Git عن مشروع `elkhamees` بتاريخ 2026-07-20. المشروع نفسه ونشراته السابقة لم تُحذف، ولم يكن عليه أي نطاق مخصص.
- **المشروع المعتمد للنشر هو `alkhamees-jewellery` فقط.** لا تعيد ربط `elkhamees` بالمستودع.

## 2026-07-27 — تنفيذ الهيكل المتفق عليه في خريطة الموقع

- **تم:** بُني الموقع بالكامل على هيكل `docs/sitemap.md`: 27 صفحة تُولّد من
  `data/catalogue.js` عبر `node tools/build-site.js`. لا تُحرّر أي صفحة يدوياً.
- **المسارات:** `/`, `/jewellery`, `/jewellery/<فئة>` (٥)، `/piece/<slug>`،
  `/occasions`, `/occasions/<slug>` (٤)، `/services`, `/about`, `/faq`,
  `/contact`، و٣ صفحات سياسات في الفوتر فقط. تُخدم بمسارات نظيفة عبر
  `cleanUrls` المفعّل أصلاً في `vercel.json`.
- **صفحات ثابتة لا صفحة واحدة:** المستند حدد مسارات حقيقية، وموقع مجوهرات يعيش
  على البحث والمشاركة؛ المسار الحقيقي يُفهرس ويُشارك بينما الـ hash لا. أُضيف
  `sitemap.xml` و`robots.txt` بعد ثبات الهيكل، كما نصّ المستند.
- **بدون عربة شراء:** نُفّذ القرار الأساسي — الموقع واجهة عرض تنتهي بمحادثة
  واتساب. لا سلة ولا دفع ولا حساب في أي صفحة، والاختبار يتحقق من غياب أي أثر لها.
- **رسالة القطعة:** زر واتساب في صفحة القطعة يفتح المحادثة وفيها **اسم القطعة
  ورقمها** تلقائياً، وهو ما طلبه المستند حرفياً.
- **الصفحة السابقة:** حُفظت في `concepts/v1-legacy.html` قبل استبدال الجذر،
  التزاماً بما وعد به README.

### قرارات العميل بتاريخ 2026-07-27

- **رقم الواتساب:** اعتُمد `966138320814` (الرقم الذي كان في الموقع) لحسم
  التعارض المسجّل بتاريخ 2026-07-20.
  ⚠️ **تحفظ مسجّل وقت الاعتماد:** الرقم بصيغة أرضي و`013` كود المنطقة الشرقية
  لا الرياض، بينما المعرض في العليا بالرياض. واتساب يعمل على رقم أرضي فقط إذا
  سُجّل كـ WhatsApp Business بتحقق عبر مكالمة. **يلزم اختباره على جهاز حقيقي
  قبل الإطلاق**، لأن كل زر في الموقع يقود إليه. تغييره من سطر واحد في
  `data/catalogue.js`.
- **الأسعار:** اعتُمد "سعر ثابت لكل قطعة". لكن الكتالوج المعتمد لم يصل، فلم
  يُخترع أي سعر: كل القطع `price: null` والموقع يعرض **"اطلب السعر"**. نشر رقم
  غير معتمد على موقع تجاري يلزم الدار بسعر لم توافق عليه. أول ما يصل الكتالوج،
  يُكتب الرقم في `price` وتظهر الأسعار في كل مكان تلقائياً.

### ما زال مطلوباً من العميل (بند ٥ في خريطة الموقع)

1. **الكتالوج المعتمد** — صور القطع بجودة عالية مع الاسم والوزن والعيار. الكتالوج
   الحالي (٨ قطع) مبدئي لبناء الهيكل ويُستبدل بالكامل.
2. **الأسعار** بعد اعتماد الكتالوج.
3. **تأكيد رقم الواتساب** باختباره فعلياً.
4. **نص "عن الدار"** — النص الحالي مؤقت من كتابتنا.
5. **صور المعرض من الداخل** — لا توجد صور داخلية في المكتبة حالياً.
6. **اعتماد العنوان وأوقات العمل** — لا تزال مأخوذة من قوقل مابس وغير معتمدة.

## 2026-07-27 — إعادة تصميم الواجهة (v2)

الطلب: «شكل الموقع عادي أوي»، مع Framer كمرجع بصري. البناء السابق كان صحيحاً
هيكلياً لكنه مسطّح بصرياً: خلفية سوداء واحدة، بطاقات متساوية، وخط صغير.

ما تغيّر (`assets/site.css` أعيدت كتابته + قوالب `tools/build-site.js`):

1. **هيرو يملأ الشاشة** (`100svh`) بصورة من الحافة للحافة وحركة ken-burns بطيئة،
   والنص فوق الصورة لا في صندوق. النص أُزيح إلى الجهة الفارغة من الكادر حتى لا
   يغطي وجه العارضة.
2. **مقياس خطّي كبير** — العنوان الرئيسي حتى `6.2rem` على سطح المكتب.
3. **شبكة خطوط شعرية 1px** تقسّم الصفحة معمارياً بدل البطاقات العائمة. الخط
   الفاتح للأقسام المعتمة والداكن للأشرطة الفاتحة.
4. **تناوب أسود/بيج** — شريط `on-light` للمجوهرات، لأنها تُقرأ أفضل على فاتح.
5. **تخطيط غير متماثل** — صورة كبيرة تقابلها قائمة الفئات (`.split`) بدل خمس
   بطاقات متساوية لا تقول أيها الأهم.
6. **بطاقة القطعة**: الاسم ← خط شعري ← السعر على سطر واحد. على الجوال يسقط الخط
   ويأخذ الاسم سطره، وإلا انكمش الاسم حتى اختفى.
7. **موتيف ✳** متكرّر، وشريط كلمات متحرك (`.marquee`).
8. **ظهور عند التمرير** (`.rv` + IntersectionObserver) يحترم
   `prefers-reduced-motion`، والهيدر شفاف فوق الهيرو ثم يعتم بعد التمرير.

قرارات مسجّلة:

- **بقي اتجاه Noir المعتمد** ولم يُستبدل بموقع فاتح كالمرجع؛ أُضيف التباين عبر
  الأشرطة الفاتحة بدل قلب الهوية التي اعتمدها العميل.
- **زر واتساب الأساسي صار ذهبياً**؛ الأخضر الفاقع كان يكسر لوحة الدار. الزر
  العائم بقي أخضر حتى تظل الإشارة مفهومة.

التحقق: 101 فحصاً في المتصفح (Chrome عبر puppeteer) على ١١ صفحة — لا أخطاء
كونسول، لا طلبات فاشلة، لا صور مكسورة، لا تجاوز أفقي على 1440 و390، ورقم
الواتساب صحيح في كل رابط.

## 2026-07-28 — اللوحة الفاتحة «شمبانيا» (v3)

- **قرار مصطفى:** قُلبت اللوحة من Noir الداكن إلى أساس فاتح، بمرجع بصري هو قالب Jewel على Framer. هذا ينسخ اعتماد Noir المسجّل في 2026-07-15 كاتجاه بصري، ولم يُعرض التغيير على العميل بعد.
- **الأساس:** خلفية ورقية `#f4f2ef` ونص `#141210`، والشريط المتناوب `#ebe6df` بدل الأسود.
- **الشمبانيا:** سطح واحد مُعاد استخدامه (`.champ`) بتدرّج `#e0cdb4 → #cbb190` فوقه طبقة تصنفير (ضوضاء SVG مضمّنة، `background-blend-mode:soft-light`). يغطّي: بطاقات الخدمات والمناسبات، شريط الثقة، الأزرار الممتلئة، والفلتر النشط.
- **بطاقات القطع:** بقيت على ورق محايد والشمبانيا تظهر عند المرور فقط — ملء البلاطة يزاحم صورة المجوهرة.
- **التباين:** الذهبي للنص صار برونزاً `#8a6534`؛ الذهبي الفاتح لا يُقرأ على خلفية فاتحة.
- **ما بقي داكناً:** الهيرو وشريط الدعوة وبلاطات المناسبات — نص أبيض فوق صورة، كما في المرجع.
- **الشعار:** يُصدَّر الآن بنسختين ويبدّل بينهما CSS (كريمي فوق الهيرو، ذهبي على الهيدر الورقي). النسخة الكريمية وحدها كانت تختفي على الخلفية الفاتحة.
- **الصفحات الداخلية:** الهيدر عليها ورقي دائماً عبر `body:has(.page-head)` — لا هيرو داكن تحته، وكان النص الفاتح يختفي قبل بدء التمرير.
- **تحقق:** أعيد بناء الـ27 صفحة، وفُحصت الرئيسية والمجوهرات والمناسبات والخدمات والتواصل بصرياً بعرض 1440px.
- **مراجعة مطلوبة:** عرض الاتجاه الجديد على العميل. صورة `assets/concept-v2/jewelry-set.jpg` خلفيتها شمبانيا محروقة بينما بقية صور القطع خلفيتها بيضاء — تظهر غير متسقة في الشبكة.

## 2026-07-28 — تناوب داكن/فاتح بدرجتين متقاربتين (v3.1)

- **قرار مصطفى:** بدل «كله فاتح»، يعود التناوب داكن/فاتح — بشرط تقريب الدرجتين من بعضهما.
- **الطرفان:** رملي دافئ `#efe9e0` مقابل بنّي محروق `#362e28`. كلاهما بنّي الأساس، فلا يقرآن كأبيض مقابل أسود.
- **التوزيع:** `.section` داكن و`.on-light` فاتح (`.band` غير مستخدم في المولّد أصلاً). الهيرو وشريط الدعوة وبلاطات المناسبات داكنة دائماً.
- **الأكسنت لونان:** برونز `#8a6534` على الفاتح، وذهبي `#e0c08a` على الداكن — لون واحد لا يُقرأ على الخلفيتين.
- **الشمبانيا:** تبقى جزيرة فاتحة في الحالتين، ونصّها حبري. استُثنيت صراحةً من قواعد القسم الداكن لأن `.section:not(.on-light) h3` أعلى تخصيصاً من `.svc h3` وكان يقلب عناوين البطاقات إلى أبيض على شمبانيا.
- **إصلاح الهيدر:** كان `fixed; top:0` بينما الشريط العلوي في السياق العادي فوقه، فيتراكب النصّان. كان مستتراً حين كان الاثنان أسودين. الآن يُزاح الهيدر بارتفاع الشريط عبر `--topbar-h` (يضبطه JS من `offsetHeight` ويحدّثه عند تغيير المقاس)، ويلتصق بالأعلى عند التمرير. تحقق: الهيدر عند 44px والشريط ينتهي عند 44.31px — بلا تراكب.
- **قائمة الجوال:** كانت `position:fixed; top:64px` (رقم سحري يفترض هيدراً ملتصقاً بالأعلى)؛ صارت `position:absolute; top:100%` معلّقة بالهيدر نفسه فتتبعه حين يُزاح.
- **تحقق:** أعيد بناء الـ27 صفحة، وفُحصت الرئيسية والمجوهرات والخدمات بصرياً بعرض 1440px، وفُحص موضع الهيدر برمجياً في حالتي السكون والتمرير.

## 2026-07-28 — الداكن يعود أسود (v3.2)

- **قرار مصطفى بعد المعاينة:** جُرّب البنّي المحروق `#362e28` لتقريب الطرفين، واختير الأسود `#0b0a09` بدلاً منه.
- **السبب البصري:** المجوهرة تنفصل عن الأسود أوضح، والأسود يطابق طبقات الهيرو وشريط الدعوة أصلاً فيختفي الخط الفاصل بينها وبين الأقسام.
- **بقي كما هو:** التناوب داكن/فاتح، والفاتح الرملي الدافئ `#efe9e0` (أدفأ من الأبيض الناصع فيلطّف التضاد)، وأسطح الشمبانيا، والأكسنت بلونيه.
- **تحقق:** أعيد بناء الـ27 صفحة وفُحصت الرئيسية بصرياً بعرض 1440px.

## 2026-07-29 — إعادة تصميم كاملة: كتالوج تحريري (v4)

- **الطلب:** مصطفى رفض شكل v3 وطلب أفضل ما يمكن الوصول إليه. استُشير Codex؛ وافق على التشخيص ووصل مستقلاً إلى الأولوية نفسها: توحيد الصور قبل أي CSS.
- **السبب الجذري:** المشكلة لم تكن الألوان بل ثلاثة أشياء — صور بخلفيات متضاربة، ومساحات ذهبية ممتلئة تقرأ كبانر، وغياب سلّم طباعي.

**١. توحيد الصور (أكبر أثر مفرد)**
- `tools/normalize-images.py` يعزل كل قطعة عن خلفيتها ويضعها على `#f2eee7` بنسبة 4:5 وهامش وظل موحّدين. الأصول لا تُمسّ؛ المخرجات في `assets/catalogue/`.
- حالتان لم يحلّهما القناع العام (`tools/fix-two-images.py`): `ring-pear.png` خلفيته الزخرفية الشمبانية داخل الـalpha فأُسقطت لونياً، و`bridal-set.jpg` بُست عرض بنفسجي أُعيد إلى رمادي دافئ محايد.
- **لقطات الأجواء** لا يمكن عزلها فوُحّدت بالتدرّج اللوني (`tools/grade-lifestyle.py`): إشباع 0.72، دفء ثابت، رفع ظلال، وتوحيد الإضاءة نحو 132 — فتقرأ الأربع كحملة واحدة. المخرجات في `assets/editorial/`.

**٢. نظام التصميم v4**
- أُلغيت كل المساحات الممتلئة: بطاقات الشمبانيا صارت خيوطاً شعرية، وشريط الثقة سطراً، والأزرار حبراً صلباً لا ذهباً.
- الفراغ بين الأقسام `clamp(6rem,12vw,11rem)`، والهيرو 82vh بدل ملء الشاشة.
- سلّم طباعي: Aref Ruqaa للعرض الكبير فقط، وTajawal وزن 300 لكل ما عداه بأحجام محدودة ومتباعدة الحروف.
- لا تناوب منتظم: الورق أساس، والأسود يظهر مرتين فقط (الهيرو والخاتمة).
- شبكة القطع بلا صناديق ولا حدود — الفراغ وحده يفصل.

**٣. قرارات اتُّخذت بعد المعاينة**
- أُلغيت «القطعة البطلة» بعرض عمودين: أنتجت صفاً أخيراً مبتوراً لأن عدد القطع متغيّر (5 في الرئيسية، 8 في المجوهرات).
- شبكة بثلاث قطع أو أقل تُقيَّد بعرض عمود أقصى وتُرصّ من البداية، وإلا بدت «قطع مشابهة» بقطعة واحدة كخلل.
- صورة القطعة: جُرّب إطار مربّع فقُصّ الألماس. **لا تُقصّ صورة قطعة أبداً** — النسبة 4:5 كالمصدر والحجم بالعرض وحده.
- **تحقق:** أعيد بناء الـ27 صفحة؛ وفُحصت الرئيسية والمجوهرات والقطعة والخدمات والمناسبات بصرياً بعرض 1440px. لقطات الصفحة الكاملة تطبع الهيدر الثابت فوق المحتوى — التحقق النهائي من لقطات إطار العرض.

## 2026-07-29 — إصلاح الهيدر المختفي + هيرو تحريري (v4.1)

- **خلل الهيدر (بلاغ مصطفى):** في صفحات القطع كان النص والشعار غير مرئيين. السبب: القاعدة الفاتحة كانت معلّقة على `body:has(.page-head)`، وصفحات القطع تستخدم `.piece` لا `.page-head` — فبقي الهيدر في وضع «فوق هيرو داكن»: نص كريمي على ورق كريمي، والشعار نسخته الكريمية.
- **الإصلاح جذري لا موضعي:** الموقع كله ورقي الآن، فلم تعد هناك حاجة لنسختين من الهيدر أصلاً. حُذف الفرع الداكن بالكامل (النص، الشعار، زر واتساب، زر القائمة) وبقيت نسخة واحدة حبرية. هذا يُنهي هذا الصنف من الأخطاء بدل ملاحقة كل صفحة جديدة.
- **الهيرو (طلب مصطفى):** الشكل السابق كان صورة تملأ الشاشة وحجاب أسود فوقها والنص عليها — قرأ كصفحة هبوط. المشكلة مركّبة: الصورة المتاحة بورتريه تجميل لا تُظهر قطعة، والحجاب أطفأ أول ما تراه العين.
- **البديل:** هيرو تحريري — النص والصورة جاران على الورق نفسه، بلا حجاب وبلا نص فوق وجه. لوحة صورة بارتفاع محكوم `clamp(400px,58vh,600px)` وتحتها تسمية كصفحة كتالوج، وخيط ذهبي يفصل العنوان عن السطر. الطباعة هي التي تحمل الصفحة.
- **أثر جانبي مقصود:** الهيدر لم يعد يعلو خلفية داكنة، فسقط سبب اختفاء النص من جذره.
- **الصور:** اختيرت `campaign-hero` للهيرو (القطعة في سياقها) بدل `model-gold` (بورتريه تجميل). ولمنع تكرار صورة الهيرو في كارت «الخطوبة»، أُدرجت `hero-collection` المتاحة وغير المستعملة — لقطة طقم ذهب — لكارت «الزواج والملكة»، وأُعيد التوزيع بلا تكرار.
- **تحقق:** فُحصت الرئيسية وصفحة القطعة بلقطات إطار عرض (لا لقطات صفحة كاملة، فهي تطبع الهيدر الثابت فوق المحتوى).

## 2026-07-29 — الهيرو: الصورة شريطاً بعرض المقطع (v4.2)

- **طلب مصطفى:** صورة الهيرو بعرض المقطع كاملاً.
- **الشكل النهائي بعد ثلاث محاولات:** (١) صورة تملأ الشاشة والنص فوقها بحجاب أسود — قرأت كصفحة هبوط. (٢) نص وصورة جاران في عمودين — الصورة ضاقت. (٣) **المعتمد:** ترويسة طباعية على الورق، ثم الصورة شريطاً من حافة النافذة إلى حافتها (`width:100vw; margin-inline:calc(50% - 50vw)`) بارتفاع محكوم `clamp(320px,54vh,580px)`، وتحتها تسمية بمحاذاة الحاوية.
- **الترويسة عمودان:** العنوان الكبير في الأول، والسطر التعريفي والأزرار في الثاني مرصوصة لأسفل — العمود الواحد كان يترك نصف العرض فارغاً فوق الصورة.
- **صورة جديدة:** `assets/editorial/campaign-hero-wide.jpg` — قصّ 2:1 من الأصل 3:2 بإزاحة لأعلى (48px) لأن القصّ المركزي يقطع الرأس، ثم نفس التدرّج اللوني الموحّد. النسخة العمودية 3:4 لم تكن تصلح لشريط عريض.
- **تحقق:** الرئيسية بلقطة إطار عرض ولقطة صفحة كاملة بعرض 1440px.

## 2026-07-29 — الهيرو: الصورة خلفية والنص فوقها (v4.3)

- **قرار مصطفى:** الصورة خلفية تحت الكلام. (الأشكال السابقة: صورة ملء الشاشة بحجاب أسود، ثم عمودان، ثم شريط عريض تحت الترويسة.)
- **ما اختلف عن المحاولة الأولى — وهو سبب فشلها آنذاك:**
  - **الحجاب حليبي لا أسود.** الصورة فاتحة ودافئة أصلاً؛ الحجاب الأسود كان يطفئها ويجعل النص الأبيض يطفو على وجه. الآن التدرّج بلون الورق نفسه والنص حبري، فيبدو امتداداً للصفحة لا طبقة فوقها.
  - **الصورة مقلوبة أفقياً** (`assets/editorial/hero-bg.jpg`): الموديل في يمين الكادر والعربية تبدأ من اليمين، فكان النص يقع على الوجه. بالعكس صار الفراغ يميناً والقطعة يساراً. لا نص داخل الكادر والقطعة متماثلة، فالعكس آمن.
  - **الصورة تُظهر القطعة** (قلادة وأقراط في سياقها) لا بورتريه تجميل.
- **حجاب علوي منفصل** (`.hero::before`): الهيدر يعلو الصورة ونصّه حبري، وفوق شعر الموديل الغامق كان يختفي. تدرّج حليبي من الأعلى يضمن قراءته بعرض الشاشة.
- **الجوال:** الحجاب يصير رأسياً من الأسفل — الشاشة الضيقة لا تتّسع لنصّ بجانب الموديل.
- **تحقق:** 1440px و390px؛ لا تمرير أفقي.

## 2026-07-29 — هيدر متجر، خلفية بيضاء، وخمس قطع في الصف (v5)

- **قرار مصطفى:** الهيدر على نمط L'Azurde، وخلفية الموقع بيضاء بدل العاجي، والشبكة خمس قطع في الصف.
- **الهيدر بصفّين:** البحث (يمين)، الشعار في الوسط، وواتساب (يسار)؛ وتحتها صفّ التنقّل بعرض الصفحة. أبيض دائماً و`sticky` بدل `fixed` الشفاف — فسقط معه حجاب الهيرو العلوي ونسخة الشعار الكريمية، ولم يعد الهيدر يحتاج إزاحة `--topbar-h`.
- **بحث حقيقي لا شكلي:** فهرس القطع يُولَّد من `data/catalogue.js` داخل كل صفحة (٢٠ قطعة)، بمطابقة تتجاهل الهمزات والتاء المربوطة والتشكيل. النتائج تنسدل تحت الحقل، وEnter يفتح أقرب قطعة، وبلا نتيجة يقترح تصفّح الكل.
- **الخلفية:** `--paper` صار `#ffffff`، و`--paper-3` (سطح بلاطة المنتج) `#f6f5f3`.
- **الصور:** `tools/retint-catalogue.py` — نقل خلفية صور الكتالوج من `#f2eee7` إلى لون البلاطة الجديد على المخرجات نفسها (الأصول غير متاحة لإعادة تشغيل `normalize-images.py`)، بإزاحة تخفّ كلما ابتعد البكسل عن الخلفية فلا تتلوّن القطعة ولا الظل.
- **الشبكة:** خمسة أعمدة على سطح المكتب، وتنزل ٤ تحت 1240px و٣ تحت 1000px وعمودين تحت 600px.
- **تحقق:** الرئيسية وصفحة المجوهرات بعرض 2560px، والهيدر والشبكة والقائمة المنسدلة بعرض 400px؛ لا تمرير أفقي ولا أخطاء متصفح.

## 2026-07-30 — كتالوج الدار الحقيقي (خمس قطع) وهوفر صورة الموديل

- **تم:** حُذف الكتالوج التجريبي (8 قطع بصور مكتبة المشروع) بالكامل، ومكانه كتالوج الدار الفعلي: خاتم الكمثرى الرباعي، قلادة الكمثرى المتدلية، قلادة المركيز العريضة، إسورة المركيز العريضة، أقراط الشلال.
- **تم:** لكل قطعة صورتان في `data/catalogue.js` — `image` صورة المنتج على خلفية بيضاء، و`hover` صورة الموديل لابسة نفس القطعة. عند المرور على البطاقة تتبدّل الصورة بتلاشٍ (على صفحة القطعة كذلك). على الجوال لا يوجد مرور بالمؤشر فالصورة الثانية لا تُعرض ولا تُحمّل.
- **تم:** الفئة بلا قطع تُستبعد تلقائياً من البناء والتنقّل (`tools/build-site.js`)، فسقطت صفحة «أطقم كاملة» ولا يبقى رابط ميت. تعود وحدها أول ما تُضاف لها قطعة.
- **بدون اختراع أرقام:** الوزن والعيار والمقاسات والشهادة لم تصل من الدار، فتُركت فارغة ولا تُطبع صفوفها في جدول المواصفات. الأسعار ما زالت `null` و«اطلب السعر».
- **الصور:** وصلت من مستودع `eclipseagency/Elkhamees-jewelry-` (ملفات `1..4.png` للمنتج و`11..44.jpeg` للموديل). أُضيفت `tools/import-catalogue-images.py` تجهّزها: صور المنتج مربّعة 1:1 فتُحاط بالخلفية لا تُقصّ (القصّ إلى 4:5 كان يأكل أطراف الأقراط)، وخلفيتها #fafafa تُزاح إلى #f7f6f4 لون البلاطة، وصور الموديل تُقصّ من الجانبين بكامل ارتفاعها. المطابقة بين المنتج وصورة الموديل تمّت من شكل القطعة نفسها لا من ترقيم الملفات (الترقيم لم يكن مزدوجاً: 22 هي الأقراط لا القلادة).
- **ناقص ولم يُخترع بديل:** لا توجد صورة موديل لقلادة المركيز العريضة (بطاقتها بلا هوفر)، ولا صورة منتج على خلفية بيضاء للإسورة (بطاقتها تستخدم صورة الموديل مؤقتاً، فتقرأ مختلفة عن باقي الشبكة).
